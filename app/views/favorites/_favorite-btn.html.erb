<% if book.favorited_by?(current_user) %>
  <!--非同期通信-->
  <%= link_to book_favorites_path(book), method: :delete, local: false do %>
    ♥<%= book.favorites.count %>
  <% end %>
<% else %>
  <!--非同期通信-->
  <%= link_to book_favorites_path(book), method: :post, local: false do %>
    ♡<%= book.favorites.count %>
  <% end %>
<% end %>
