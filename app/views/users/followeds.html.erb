<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-11'>
      <h2>Follower Users</h2>
      <% if @user.followeds.exists? %>
        <%= render 'relationship', users: @users %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>